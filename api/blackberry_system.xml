<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE concept PUBLIC "-//IXIA//DTD IXIA DITA Composite//EN" "../../system/dtd/ixia/IxiaDitabase.dtd">
<concept id="blackbery_system" xml:lang="en-us">
  <title>System</title>
  <shortdesc><i>The System object allows you to get access to system level functions and attributes of the BlackBerry Device.</i></shortdesc>
  <conbody>
    <p>The System object is static; all of its functions and properties are used directly from the object.</p>
    <example>
      <title>Example:</title>
        <p><pre scale="80">
        &lt;script type="text/javascript"&gt;
        // See if we are in coverage
        if (!blackberry.system.hasDataCoverage()) {
          alert("You are not in coverage, we will attempt to send later");
        }

        // Check if we have GPS capability
        if (blackberry.system.hasCapability("location.gps")) {
          alert("This Device has a GPS.");
        }
        &lt;/script&gt;
        </pre></p>
    </example>
  </conbody>
</concept>
